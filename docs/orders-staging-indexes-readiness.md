# Orders Staging Indexes Readiness

## Статус

`Not executed`

## Причина

Batch D не запускался и не оценивался на реальном staging, потому что staging-контур не обнаружен.

## Что известно по текущим данным

По production read-only audit:

- индексы `orders(created_at)` отсутствуют;
- индексы `orders(status)` отсутствуют;
- индексы `orders(payment_status)` отсутствуют;
- индексы `orders(user_id)` отсутствуют;
- индекс `order_items(order_id)` отсутствует;
- `order_history` и `order_comments` отсутствуют целиком, поэтому и индексов на них нет.

## Предварительная оценка

Batch D потенциально нужен, но:

- применять его без staging нельзя;
- применять его вместе со структурными batch A/B/C не рекомендуется;
- сначала нужно поднять staging-контур, применить A/B/C и проверить smoke;
- только потом отдельно принимать решение по Batch D.

## Рекомендация

- Batch D пока не применять.
- Оценку времени выполнения и эффекта на список заказов делать только после появления staging-копии.
